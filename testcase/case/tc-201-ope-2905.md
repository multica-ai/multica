# TC-201: PDF 导出支持 Task List + Autolink + 删除线样式（OPE-2905）

## 关联信息

- **OPE 编号**: OPE-2905
- **Gitee PR**: !407, !397
- **Commit SHA**: a7acc2306, 6482e1baa, 7efba57ae, 74ffc0db7
- **特性摘要**: PDF 导出支持 Task List + Autolink + 删除线样式

## 涉及源文件

- `packages/core/api/client.ts`
- `packages/views/issues/components/markdown-preview-drawer.tsx`
- `server/cmd/server/router.go`
- `server/internal/handler/issue_export.go`
- `server/internal/handler/issue_export_test.go`

## 验证要点

1. 在包含该功能的环境中执行 OPE-2905 对应主流程，确认用户可见行为与特性摘要一致。
2. 覆盖首个 commit 引入的核心路径，并回归后续 fix/perf/test commit 中修复过的边界行为。
3. 对涉及 API/CLI/后台任务的改动，验证成功路径、权限/参数校验、错误提示和幂等性。
4. 对涉及 UI 的改动，验证中英文界面、刷新/切换/实时更新后的状态保持，以及移动端或窄屏下无布局回退。
5. 对涉及迁移、存储、通知或运行时的改动，验证向后兼容、重复执行和失败恢复。

## 备注

- 本用例由基线同步 Autopilot 根据已合入 origin/main、但未在 testcase 中显式引用的 Fork OPE 主题生成；后续可按真实验收结果继续细化步骤。
