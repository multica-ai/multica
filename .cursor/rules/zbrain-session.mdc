---
description: zbrain session routing — prefer sessions/<chat-id>.md
alwaysApply: true
---

# zbrain session

When the workspace has `.secondbrain` **or** user says 继续 / 推进 / scratch：

1. Resolve chat-id from this conversation transcript id when available
2. Route per Vault `10-SYSTEM/HARNESS/session-protocol.md`
3. Prefer `/zbrain-continue` behavior; never steal another chat's next
4. Optional: run `zb init` + `zb advance` from Vault scripts

Generated by `zbrain-harness-sync`. Edit Vault `projection/` then re-sync.
