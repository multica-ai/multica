# Placeholder until agentrunner-runtime diverges from devenv-runtime.
# When devenv-runtime management is dissociated from this repo, replace this
# FROM with a direct build from agent-runtime-base.
ARG DEVENV_IMAGE=ghcr.io/g2crowd/devenv-runtime
ARG DEVENV_TAG=latest
FROM ${DEVENV_IMAGE}:${DEVENV_TAG}
