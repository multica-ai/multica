apiVersion: v2
name: wallts
description: |
  AI-native task management — like Linear, but with AI agents as first-class citizens.
  Self-host the backend, frontend, and PostgreSQL (pgvector) on a Kubernetes cluster.
  Supports external PostgreSQL via postgresql.enabled=false.
type: application
version: 0.1.0
appVersion: "latest"
home: https://github.com/dwickyfp/wallts
sources:
  - https://github.com/dwickyfp/wallts
keywords:
  - wallts
  - issue-tracker
  - ai-agents
  - self-host
