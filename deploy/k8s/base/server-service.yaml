apiVersion: v1
kind: Service
metadata:
  name: multica-server
  labels:
    app.kubernetes.io/name: multica
    app.kubernetes.io/component: server
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: multica
    app.kubernetes.io/component: server
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
