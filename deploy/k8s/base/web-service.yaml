apiVersion: v1
kind: Service
metadata:
  name: multica-web
  labels:
    app.kubernetes.io/name: multica
    app.kubernetes.io/component: web
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: multica
    app.kubernetes.io/component: web
  ports:
    - name: http
      port: 3000
      targetPort: http
      protocol: TCP
