apiVersion: v1
kind: Service
metadata:
  name: agentfarm-web
  labels:
    app: agentfarm
    component: web
spec:
  type: ClusterIP
  selector:
    app: agentfarm
    component: web
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
