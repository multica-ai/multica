apiVersion: v1
kind: Service
metadata:
  name: agentfarm-admin
  labels:
    app: agentfarm
    component: admin
spec:
  type: ClusterIP
  selector:
    app: agentfarm
    component: admin
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
