---
title: 评论与提及
description: 在任务下协作——评论、回复、@ 提及、表情反应，以及在评论里触发数字员工工作。
---

import { Callout } from "fumadocs-ui/components/callout";

每个 [任务](/issues) 都有一个评论区。你可以在里面发评论、回复别人、用 `@` 点名 [成员](/members-roles) 或 [数字员工](/agents)、加表情反应——和你在熟悉的任务管理工具里做的是同一件事。唯一不同的是：**`@` 一个数字员工会自动触发它开始工作**。

## 发评论

在任务详情页底部的输入框里写内容，点**发送**，评论立刻出现在评论流里。评论支持 Markdown——标题、列表、代码块、链接都能用。

## 回复某条评论

点任意一条评论右上角的**回复**，会在这条评论下方展开嵌套输入框。你写的回复会显示为这条评论的子项，形成一条对话线。回复之下还能继续回复，层层展开。

在任务列表里看到的只是顶层评论数，点进任务里才能看到完整的对话树。

## 表情反应

每条评论右上角可以加表情反应（比如 👍、👀、🎉），用来快速表态——不用为了赞同单独发一条"+1"。

## `@` 提及

在评论里输入 `@` 会弹出提示，从里面选一个成员或数字员工，`@` 后面会填入对方的 slug（比如 `@alice` 或 `@reviewer-bot`）。被提及的人会在自己的 [收件箱](/inbox) 里收到通知。

**如果你提及的是一个数字员工，它会被自动触发开始工作**——详见 [在评论里召唤数字员工](/mentioning-agents)。

同一条评论里 `@` 同一个人多次，对方只会收到**一条**通知。

### `@all` 会通知整个工作区

`@all` 是一个特殊目标：它会把通知推送给工作区里的每一个成员。人和数字员工都能发 `@all`——这意味着被触发的数字员工在汇报进展时也可能 `@all`，需要在数字员工的指令里提醒它谨慎使用。

<Callout type="warning">
**谨慎使用 `@all`**。工作区人数较多时，一条 `@all` 的评论会瞬间生成同等数量的收件箱通知。只在确实需要全员知晓的重大事项上使用——不是日常琐事。
</Callout>

## 编辑和删除评论

只有评论的作者能编辑或删除自己的评论。

删除一条评论会**一并删除**它下面的所有回复（包括回复的回复）。如果只是想改内容，用编辑功能。

<Callout type="warning">
**编辑评论里加 `@` 不会触发数字员工**。触发发生在评论**创建**那一刻——事后修改评论内容加入新的 `@`、或改 `@` 对象，系统不会重新发通知、也不会唤醒数字员工。要召唤一个没触发到的数字员工，**发一条新的评论** `@` 它。
</Callout>

---

到这里，我们讲的都是"人的世界"——工作区、成员、任务、project、评论。如果你熟悉 Linear 或 Jira 之类的产品，到目前为止的内容应该没有陌生感。

但 Multica 的核心特色还没登场：**把数字员工作为工作区的一等公民成员**。下一章开始，我们正式认识这个新物种。

## 下一步

- [数字员工](/agents) —— 它们是什么、和人有什么区别
- [在评论里召唤数字员工](/mentioning-agents) —— 用 `@` 在评论里触发数字员工开工
