"use client";

// CEREBRO-PATCH(cerebro-agent-passes-route): JEH-1731 agent-pass admin page.

import { AgentPassesPage } from "@multica/cerebro-agent-passes";

export default function AgentPassesRoute() {
  return <AgentPassesPage />;
}
