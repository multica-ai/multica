// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: skill_version.sql

package db

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const createSkillVersion = `-- name: CreateSkillVersion :one
INSERT INTO skill_version (skill_id, version, content, files, description, created_by)
VALUES ($1, $2, $3, $4, $5, $6)
RETURNING id, skill_id, version, content, files, description, created_by, created_at
`

type CreateSkillVersionParams struct {
	SkillID     pgtype.UUID `json:"skill_id"`
	Version     string      `json:"version"`
	Content     string      `json:"content"`
	Files       []byte      `json:"files"`
	Description string      `json:"description"`
	CreatedBy   pgtype.UUID `json:"created_by"`
}

func (q *Queries) CreateSkillVersion(ctx context.Context, arg CreateSkillVersionParams) (SkillVersion, error) {
	row := q.db.QueryRow(ctx, createSkillVersion,
		arg.SkillID,
		arg.Version,
		arg.Content,
		arg.Files,
		arg.Description,
		arg.CreatedBy,
	)
	var i SkillVersion
	err := row.Scan(
		&i.ID,
		&i.SkillID,
		&i.Version,
		&i.Content,
		&i.Files,
		&i.Description,
		&i.CreatedBy,
		&i.CreatedAt,
	)
	return i, err
}

const getSkillVersion = `-- name: GetSkillVersion :one
SELECT id, skill_id, version, content, files, description, created_by, created_at FROM skill_version
WHERE skill_id = $1 AND version = $2
`

type GetSkillVersionParams struct {
	SkillID pgtype.UUID `json:"skill_id"`
	Version string      `json:"version"`
}

func (q *Queries) GetSkillVersion(ctx context.Context, arg GetSkillVersionParams) (SkillVersion, error) {
	row := q.db.QueryRow(ctx, getSkillVersion, arg.SkillID, arg.Version)
	var i SkillVersion
	err := row.Scan(
		&i.ID,
		&i.SkillID,
		&i.Version,
		&i.Content,
		&i.Files,
		&i.Description,
		&i.CreatedBy,
		&i.CreatedAt,
	)
	return i, err
}

const listSkillVersions = `-- name: ListSkillVersions :many

SELECT id, skill_id, version, content, files, description, created_by, created_at FROM skill_version
WHERE skill_id = $1
ORDER BY created_at DESC
`

// Skill version snapshots (append-only). Every merged change creates a row.
func (q *Queries) ListSkillVersions(ctx context.Context, skillID pgtype.UUID) ([]SkillVersion, error) {
	rows, err := q.db.Query(ctx, listSkillVersions, skillID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []SkillVersion{}
	for rows.Next() {
		var i SkillVersion
		if err := rows.Scan(
			&i.ID,
			&i.SkillID,
			&i.Version,
			&i.Content,
			&i.Files,
			&i.Description,
			&i.CreatedBy,
			&i.CreatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
