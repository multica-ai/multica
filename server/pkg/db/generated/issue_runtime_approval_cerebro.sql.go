// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: issue_runtime_approval_cerebro.sql

package db

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const getOpenRuntimeApprovalIssue = `-- name: GetOpenRuntimeApprovalIssue :one

SELECT id, workspace_id, title, description, status, priority, assignee_type, assignee_id, creator_type, creator_id, parent_issue_id, acceptance_criteria, context_refs, position, due_date, created_at, updated_at, number, project_id, origin_type, origin_id, first_executed_at, kind, is_private, classification FROM issue
WHERE workspace_id = $1
  AND origin_type = 'runtime_approval'
  AND origin_id = $2
  AND status NOT IN ('done', 'cancelled')
ORDER BY created_at DESC
LIMIT 1
`

type GetOpenRuntimeApprovalIssueParams struct {
	WorkspaceID pgtype.UUID `json:"workspace_id"`
	OriginID    pgtype.UUID `json:"origin_id"`
}

// CEREBRO-PATCH(runtime-approval-issue): JEH-1152 — auto-create / reuse an
// "Approve runtime" issue when a non-admin's agent-create gets gated on the
// group runtime allowlist. Net-new file; query lives outside the upstream
// issue.sql to keep the cerebro carve-out isolated.
// Finds the existing open approval issue for (workspace_id, runtime_id). The
// approval-issue helper stamps (origin_type='runtime_approval', origin_id=
// runtime_id) on creation so this lookup can dedupe. "Open" = status NOT IN
// ('done', 'cancelled'); reusing a cancelled/done issue would silently
// revive a request the admin already actioned.
func (q *Queries) GetOpenRuntimeApprovalIssue(ctx context.Context, arg GetOpenRuntimeApprovalIssueParams) (Issue, error) {
	row := q.db.QueryRow(ctx, getOpenRuntimeApprovalIssue, arg.WorkspaceID, arg.OriginID)
	var i Issue
	err := row.Scan(
		&i.ID,
		&i.WorkspaceID,
		&i.Title,
		&i.Description,
		&i.Status,
		&i.Priority,
		&i.AssigneeType,
		&i.AssigneeID,
		&i.CreatorType,
		&i.CreatorID,
		&i.ParentIssueID,
		&i.AcceptanceCriteria,
		&i.ContextRefs,
		&i.Position,
		&i.DueDate,
		&i.CreatedAt,
		&i.UpdatedAt,
		&i.Number,
		&i.ProjectID,
		&i.OriginType,
		&i.OriginID,
		&i.FirstExecutedAt,
		&i.Kind,
		&i.IsPrivate,
		&i.Classification,
	)
	return i, err
}
