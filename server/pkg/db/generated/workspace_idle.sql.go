// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: workspace_idle.sql

package db

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const claimWorkspaceIdleNotification = `-- name: ClaimWorkspaceIdleNotification :one
UPDATE workspace_idle_state
SET idle_notified_generation = busy_generation,
    updated_at = now()
WHERE workspace_id = $1
  AND idle_notified_generation < busy_generation
RETURNING busy_generation
`

func (q *Queries) ClaimWorkspaceIdleNotification(ctx context.Context, workspaceID pgtype.UUID) (int64, error) {
	row := q.db.QueryRow(ctx, claimWorkspaceIdleNotification, workspaceID)
	var busy_generation int64
	err := row.Scan(&busy_generation)
	return busy_generation, err
}

const countWorkflowInProgressIssues = `-- name: CountWorkflowInProgressIssues :one
SELECT count(*)
FROM issue
WHERE workspace_id = $1
  AND issue_effective_status(workspace_id, status) = 'in_progress'
`

func (q *Queries) CountWorkflowInProgressIssues(ctx context.Context, workspaceID pgtype.UUID) (int64, error) {
	row := q.db.QueryRow(ctx, countWorkflowInProgressIssues, workspaceID)
	var count int64
	err := row.Scan(&count)
	return count, err
}

const ensureWorkspaceWorkflowBusy = `-- name: EnsureWorkspaceWorkflowBusy :exec
INSERT INTO workspace_idle_state (
    workspace_id,
    busy_generation,
    idle_notified_generation,
    updated_at
) VALUES ($1, 1, 0, now())
ON CONFLICT (workspace_id) DO NOTHING
`

// A terminal event can be the first lifecycle signal observed after a rolling
// deployment or process restart. Seed only a missing row; unlike Mark*, this
// never advances a generation for a duplicate terminal event.
func (q *Queries) EnsureWorkspaceWorkflowBusy(ctx context.Context, workspaceID pgtype.UUID) error {
	_, err := q.db.Exec(ctx, ensureWorkspaceWorkflowBusy, workspaceID)
	return err
}

const hasActiveTasksInWorkspace = `-- name: HasActiveTasksInWorkspace :one
SELECT EXISTS (
    SELECT 1
    FROM agent_task_queue task
    JOIN agent a ON a.id = task.agent_id
    WHERE a.workspace_id = $1
      AND task.status IN ('queued', 'dispatched', 'running', 'waiting_local_directory')
) AS has_active
`

func (q *Queries) HasActiveTasksInWorkspace(ctx context.Context, workspaceID pgtype.UUID) (bool, error) {
	row := q.db.QueryRow(ctx, hasActiveTasksInWorkspace, workspaceID)
	var has_active bool
	err := row.Scan(&has_active)
	return has_active, err
}

const listWorkflowIdleRecipientUserIDs = `-- name: ListWorkflowIdleRecipientUserIDs :many
WITH in_progress_issues AS MATERIALIZED (
    SELECT id, creator_type, creator_id
    FROM issue
    WHERE workspace_id = $1
      AND issue_effective_status(workspace_id, status) = 'in_progress'
), recipient_candidates AS (
    SELECT creator_id AS user_id
    FROM in_progress_issues
    WHERE creator_type = 'member'

    UNION ALL

    SELECT subscriber.user_id
    FROM in_progress_issues issue
    JOIN issue_subscriber subscriber ON subscriber.issue_id = issue.id
    WHERE subscriber.user_type = 'member'
      AND subscriber.reason = 'delegated'
      AND subscriber.unsubscribed_at IS NULL
)
SELECT DISTINCT candidate.user_id
FROM recipient_candidates candidate
JOIN member m ON m.workspace_id = $1 AND m.user_id = candidate.user_id
ORDER BY candidate.user_id
`

func (q *Queries) ListWorkflowIdleRecipientUserIDs(ctx context.Context, workspaceID pgtype.UUID) ([]pgtype.UUID, error) {
	rows, err := q.db.Query(ctx, listWorkflowIdleRecipientUserIDs, workspaceID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []pgtype.UUID{}
	for rows.Next() {
		var user_id pgtype.UUID
		if err := rows.Scan(&user_id); err != nil {
			return nil, err
		}
		items = append(items, user_id)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const lockWorkspaceForIdleNotification = `-- name: LockWorkspaceForIdleNotification :one
SELECT id FROM workspace
WHERE id = $1
FOR UPDATE
`

// Task inserts take FOR KEY SHARE on this row through lock_task_owner_rows.
// Holding FOR UPDATE until inbox rows commit makes the final active-task check
// and the busy -> idle edge atomic with respect to the next task creation.
func (q *Queries) LockWorkspaceForIdleNotification(ctx context.Context, id pgtype.UUID) (pgtype.UUID, error) {
	row := q.db.QueryRow(ctx, lockWorkspaceForIdleNotification, id)
	var id_2 pgtype.UUID
	err := row.Scan(&id_2)
	return id_2, err
}

const markWorkspaceWorkflowBusy = `-- name: MarkWorkspaceWorkflowBusy :exec
INSERT INTO workspace_idle_state (
    workspace_id,
    busy_generation,
    idle_notified_generation,
    updated_at
) VALUES ($1, 1, 0, now())
ON CONFLICT (workspace_id) DO UPDATE
SET busy_generation = CASE
        WHEN workspace_idle_state.idle_notified_generation = workspace_idle_state.busy_generation
        THEN workspace_idle_state.busy_generation + 1
        ELSE workspace_idle_state.busy_generation
    END,
    updated_at = now()
`

// The first active task after an idle notification starts a new generation.
// Further task events in the same busy period leave that generation unchanged.
func (q *Queries) MarkWorkspaceWorkflowBusy(ctx context.Context, workspaceID pgtype.UUID) error {
	_, err := q.db.Exec(ctx, markWorkspaceWorkflowBusy, workspaceID)
	return err
}
