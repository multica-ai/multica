# server/internal/cerebro/mcp

Cerebro-fork MCP guide / integration backend (cerebro-specific MCP endpoints + helpers).

**Owns:** Cerebro MCP guide endpoints, cerebro-side MCP tool wiring beyond upstream.

**May land here:** MCP guide content, cerebro-only tool registration helpers.

**May NOT land here:** Upstream MCP server core (lives in `server/internal/mcp`).

**Naming:** snake_case files.

**Upstream-import-niveau:** L4 (cerebro-only).
