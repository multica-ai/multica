// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: chat_message_cost.sql

package cerebrodb

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const getChatSessionMessageCosts = `-- name: GetChatSessionMessageCosts :many
SELECT
    tu.task_id,
    tu.model,
    COALESCE(SUM(tu.input_tokens), 0)::bigint       AS total_input_tokens,
    COALESCE(SUM(tu.output_tokens), 0)::bigint      AS total_output_tokens,
    COALESCE(SUM(tu.cache_read_tokens), 0)::bigint  AS total_cache_read_tokens,
    COALESCE(SUM(tu.cache_write_tokens), 0)::bigint AS total_cache_write_tokens,
    COALESCE(SUM(tu.cost_cents), 0)::bigint          AS total_cost_cents
FROM task_usage tu
JOIN agent_task_queue atq ON atq.id = tu.task_id
WHERE atq.chat_session_id = $1
GROUP BY tu.task_id, tu.model
`

type GetChatSessionMessageCostsRow struct {
	TaskID                pgtype.UUID `json:"task_id"`
	Model                 string      `json:"model"`
	TotalInputTokens      int64       `json:"total_input_tokens"`
	TotalOutputTokens     int64       `json:"total_output_tokens"`
	TotalCacheReadTokens  int64       `json:"total_cache_read_tokens"`
	TotalCacheWriteTokens int64       `json:"total_cache_write_tokens"`
	TotalCostCents        int64       `json:"total_cost_cents"`
}

// Per-task token totals + cost for every task in this chat session, so the
// chat UI can render a price badge under each assistant reply (FIR-31). Keyed
// by task_id (= chat_message.task_id) and split by model so pricing stays
// correct when a single run spans models. The handler sums per task and
// prefers the gateway-reported spend over the pricing-table estimate, exactly
// like the session total in GetChatSessionUsage.
func (q *Queries) GetChatSessionMessageCosts(ctx context.Context, chatSessionID pgtype.UUID) ([]GetChatSessionMessageCostsRow, error) {
	rows, err := q.db.Query(ctx, getChatSessionMessageCosts, chatSessionID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []GetChatSessionMessageCostsRow{}
	for rows.Next() {
		var i GetChatSessionMessageCostsRow
		if err := rows.Scan(
			&i.TaskID,
			&i.Model,
			&i.TotalInputTokens,
			&i.TotalOutputTokens,
			&i.TotalCacheReadTokens,
			&i.TotalCacheWriteTokens,
			&i.TotalCostCents,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
