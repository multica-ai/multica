// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: note_lock.sql

package cerebrodb

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const acquireNoteLock = `-- name: AcquireNoteLock :one
UPDATE cerebro_note
SET locked_by         = $2,
    locked_at         = CASE WHEN locked_by = $2 THEN COALESCE(locked_at, now()) ELSE now() END,
    lock_heartbeat_at = now(),
    updated_at        = now()
WHERE artifact_id = $1
  AND (locked_by IS NULL OR locked_by = $2 OR lock_heartbeat_at IS NULL OR lock_heartbeat_at < $3)
RETURNING artifact_id, locked_by, locked_at, lock_heartbeat_at
`

type AcquireNoteLockParams struct {
	ArtifactID      pgtype.UUID        `json:"artifact_id"`
	LockedBy        pgtype.UUID        `json:"locked_by"`
	LockHeartbeatAt pgtype.Timestamptz `json:"lock_heartbeat_at"`
}

type AcquireNoteLockRow struct {
	ArtifactID      pgtype.UUID        `json:"artifact_id"`
	LockedBy        pgtype.UUID        `json:"locked_by"`
	LockedAt        pgtype.Timestamptz `json:"locked_at"`
	LockHeartbeatAt pgtype.Timestamptz `json:"lock_heartbeat_at"`
}

// Acquire (or refresh, or re-acquire) the lock for $2 (the caller). Succeeds —
// returns a row — only when the lock is free, already held by the caller, or
// stale (heartbeat older than the cutoff $3). On a fresh acquire locked_at is
// set; on re-acquire by the same holder it is preserved. Returns no rows when a
// different user holds a LIVE lock (the caller must then take over or wait).
func (q *Queries) AcquireNoteLock(ctx context.Context, arg AcquireNoteLockParams) (AcquireNoteLockRow, error) {
	row := q.db.QueryRow(ctx, acquireNoteLock, arg.ArtifactID, arg.LockedBy, arg.LockHeartbeatAt)
	var i AcquireNoteLockRow
	err := row.Scan(
		&i.ArtifactID,
		&i.LockedBy,
		&i.LockedAt,
		&i.LockHeartbeatAt,
	)
	return i, err
}

const forceAcquireNoteLock = `-- name: ForceAcquireNoteLock :one
UPDATE cerebro_note
SET locked_by         = $2,
    locked_at         = now(),
    lock_heartbeat_at = now(),
    updated_at        = now()
WHERE artifact_id = $1
RETURNING artifact_id, locked_by, locked_at, lock_heartbeat_at
`

type ForceAcquireNoteLockParams struct {
	ArtifactID pgtype.UUID `json:"artifact_id"`
	LockedBy   pgtype.UUID `json:"locked_by"`
}

type ForceAcquireNoteLockRow struct {
	ArtifactID      pgtype.UUID        `json:"artifact_id"`
	LockedBy        pgtype.UUID        `json:"locked_by"`
	LockedAt        pgtype.Timestamptz `json:"locked_at"`
	LockHeartbeatAt pgtype.Timestamptz `json:"lock_heartbeat_at"`
}

// Take over: steal the lock unconditionally for $2. Used by the "Take over"
// action when a live lock is held by someone else.
func (q *Queries) ForceAcquireNoteLock(ctx context.Context, arg ForceAcquireNoteLockParams) (ForceAcquireNoteLockRow, error) {
	row := q.db.QueryRow(ctx, forceAcquireNoteLock, arg.ArtifactID, arg.LockedBy)
	var i ForceAcquireNoteLockRow
	err := row.Scan(
		&i.ArtifactID,
		&i.LockedBy,
		&i.LockedAt,
		&i.LockHeartbeatAt,
	)
	return i, err
}

const getNoteLock = `-- name: GetNoteLock :one

SELECT artifact_id, locked_by, locked_at, lock_heartbeat_at
FROM cerebro_note
WHERE artifact_id = $1
`

type GetNoteLockRow struct {
	ArtifactID      pgtype.UUID        `json:"artifact_id"`
	LockedBy        pgtype.UUID        `json:"locked_by"`
	LockedAt        pgtype.Timestamptz `json:"locked_at"`
	LockHeartbeatAt pgtype.Timestamptz `json:"lock_heartbeat_at"`
}

// Wave 3 / G3 (TECH-3556) — interim edit lock on a Note.
// See migration 9079_cerebro_note_lock for the heartbeat + timeout model.
// The "is the lock live?" decision is taken in the handler by comparing
// lock_heartbeat_at against now()-timeout; these queries just read and mutate
// the three lock columns on cerebro_note.
func (q *Queries) GetNoteLock(ctx context.Context, artifactID pgtype.UUID) (GetNoteLockRow, error) {
	row := q.db.QueryRow(ctx, getNoteLock, artifactID)
	var i GetNoteLockRow
	err := row.Scan(
		&i.ArtifactID,
		&i.LockedBy,
		&i.LockedAt,
		&i.LockHeartbeatAt,
	)
	return i, err
}

const heartbeatNoteLock = `-- name: HeartbeatNoteLock :one
UPDATE cerebro_note
SET lock_heartbeat_at = now(),
    updated_at        = now()
WHERE artifact_id = $1 AND locked_by = $2
RETURNING artifact_id, locked_by, locked_at, lock_heartbeat_at
`

type HeartbeatNoteLockParams struct {
	ArtifactID pgtype.UUID `json:"artifact_id"`
	LockedBy   pgtype.UUID `json:"locked_by"`
}

type HeartbeatNoteLockRow struct {
	ArtifactID      pgtype.UUID        `json:"artifact_id"`
	LockedBy        pgtype.UUID        `json:"locked_by"`
	LockedAt        pgtype.Timestamptz `json:"locked_at"`
	LockHeartbeatAt pgtype.Timestamptz `json:"lock_heartbeat_at"`
}

// Keep the lock alive. Only the current holder may heartbeat; returns no rows
// if the caller no longer holds it (e.g. it was taken over), so the client
// knows to drop to read-only.
func (q *Queries) HeartbeatNoteLock(ctx context.Context, arg HeartbeatNoteLockParams) (HeartbeatNoteLockRow, error) {
	row := q.db.QueryRow(ctx, heartbeatNoteLock, arg.ArtifactID, arg.LockedBy)
	var i HeartbeatNoteLockRow
	err := row.Scan(
		&i.ArtifactID,
		&i.LockedBy,
		&i.LockedAt,
		&i.LockHeartbeatAt,
	)
	return i, err
}

const releaseNoteLock = `-- name: ReleaseNoteLock :exec
UPDATE cerebro_note
SET locked_by         = NULL,
    locked_at         = NULL,
    lock_heartbeat_at = NULL,
    updated_at        = now()
WHERE artifact_id = $1 AND locked_by = $2
`

type ReleaseNoteLockParams struct {
	ArtifactID pgtype.UUID `json:"artifact_id"`
	LockedBy   pgtype.UUID `json:"locked_by"`
}

// Release the lock, but only if the caller holds it (a stale takeover must not
// be cleared by the previous holder's late "close").
func (q *Queries) ReleaseNoteLock(ctx context.Context, arg ReleaseNoteLockParams) error {
	_, err := q.db.Exec(ctx, releaseNoteLock, arg.ArtifactID, arg.LockedBy)
	return err
}
