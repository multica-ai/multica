// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: capability_drift.sql

package cerebrodb

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const listCerebroWorkspaceOwnerAdmins = `-- name: ListCerebroWorkspaceOwnerAdmins :many
SELECT user_id FROM member
WHERE workspace_id = $1
  AND role IN ('owner', 'admin')
ORDER BY (role = 'owner') DESC, created_at ASC
`

// The user IDs of every owner/admin in the workspace — the recipients of a
// drift alert (drift is a security signal only admins can act on). Owners first.
func (q *Queries) ListCerebroWorkspaceOwnerAdmins(ctx context.Context, workspaceID pgtype.UUID) ([]pgtype.UUID, error) {
	rows, err := q.db.Query(ctx, listCerebroWorkspaceOwnerAdmins, workspaceID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []pgtype.UUID{}
	for rows.Next() {
		var user_id pgtype.UUID
		if err := rows.Scan(&user_id); err != nil {
			return nil, err
		}
		items = append(items, user_id)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listWorkspacesWithCapabilityDriftWatcher = `-- name: ListWorkspacesWithCapabilityDriftWatcher :many

SELECT workspace_id FROM cerebro_feature_flags
WHERE user_id = '00000000-0000-0000-0000-000000000000'
  AND flag_key = 'cerebro_capability_drift_watcher'
  AND enabled = TRUE
`

// TECH-3738 Bid C — capability drift watcher.
//
// The watcher periodically checks each agent for capability DRIFT: a tool the
// agent actually used (observed, Bid B) that its declared policy does not allow
// (blocked or unmapped). When drift is found it alerts the workspace's
// owners/admins. It is gated by the cerebro_capability_drift_watcher flag, which
// defaults OFF, so the watcher does nothing until an admin turns it on.
// Workspace IDs where the drift watcher flag is ON at the workspace level
// (user_id = all-zero sentinel = the workspace-level override row). Never-set =
// absent = default OFF, so the watcher simply skips the workspace.
func (q *Queries) ListWorkspacesWithCapabilityDriftWatcher(ctx context.Context) ([]pgtype.UUID, error) {
	rows, err := q.db.Query(ctx, listWorkspacesWithCapabilityDriftWatcher)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []pgtype.UUID{}
	for rows.Next() {
		var workspace_id pgtype.UUID
		if err := rows.Scan(&workspace_id); err != nil {
			return nil, err
		}
		items = append(items, workspace_id)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
