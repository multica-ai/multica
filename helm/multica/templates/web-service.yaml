apiVersion: v1
kind: Service
metadata:
  name: {{ include "multica.fullname" . }}-web
  labels:
    {{- include "multica.labels" . | nindent 4 }}
    app.kubernetes.io/component: web
spec:
  type: {{ .Values.web.service.type }}
  ports:
    - port: {{ .Values.web.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "multica.webSelectorLabels" . | nindent 4 }}
