# 单能力 Research

## 调研目标

1. 确认仓库中是否存在屏蔽列表的任何实现残片。
2. 判断 7.1 更接近产品当前能力，还是系统级边界假设。
3. 为后续是否继续推进提供证据。

## 现状链路

1. 入口：`apps/workspace/src/router.tsx` 当前没有 blocklist 路由。
2. 搜索结果：前端与后端都未找到 blocklist、网站屏蔽、应用屏蔽相关实现。
3. 产品边界：现有产品文档聚焦任务、时间、提醒、项目与 AI 协作，没有系统级阻断能力说明。
4. 输出结果：7.1 当前属于产品边界假设，而不是待补全的代码空位。

## 关键证据

| 路径 | 符号 | 结论 |
| --- | --- | --- |
| `apps/workspace/src/router.tsx` | `routeTree` | 应用内没有 blocklist 相关路由入口。 |
| `product-overview.md` | `当前阶段的项目目标与展望` | 当前阶段目标没有系统级屏蔽网站/应用的表述。 |
| `product-overview.md` | `4.5 Workspace 承接哪些能力` | workspace 侧强调任务、仓库、AI、协作上下文，不包含系统阻断策略。 |

## 空搜索证据

| 路径 | 符号 / 搜索关键词 | 结论 |
| --- | --- | --- |
| `apps/workspace/src`、`server` | `rg(blocklist|website block|app block|custom block rule)` | 未找到匹配，说明代码没有屏蔽列表实现。 |
| `apps/workspace/src`、`server` | `rg(屏蔽列表|网站屏蔽|应用屏蔽)` | 未找到匹配，说明中文命名路径下也无残片。 |

## 数据模型或状态流

- 当前不存在 blocklist 实体、规则模型、运行时分发或应用入口。
- 这意味着 7.1 若要成立，至少需要新增规则模型、系统运行时与用户配置边界。

## 边界条件

- 证据：`apps/workspace/src/router.tsx` `routeTree`；结论：纯 Web 路由层无法单独完成系统级网站或应用阻断。
- 证据：`product-overview.md` `当前阶段的项目目标与展望`；结论：即使未来需要专注支持，7.1 也不是当前阶段主轴。

## 未决问题

1. 屏蔽列表是否真的属于本产品，而非外部专注工具集成能力。
2. 若未来进入范围，是只屏蔽 Web 域名，还是也要屏蔽桌面应用；当前没有任何运行时证据支持后者。
3. 是否接受该能力必须依赖 daemon / OS 权限；当前无答案。
