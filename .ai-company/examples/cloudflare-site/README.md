# cloudflare-site — AI 公司 Cloudflare 产品线示例

**栈约束：** 仅 Cloudflare（Pages + Workers + Wrangler）。不用 Vercel / 不用生产 Docker。

由 `scripts/ai-company/scaffold-cloudflare.sh` 或 `site-factory.sh` 生成真实项目时，会复制并定制本目录下的 `brief.md` / `backlog.md` / `accept_cases.md`。
