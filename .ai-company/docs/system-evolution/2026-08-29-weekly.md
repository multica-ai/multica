# 系统进化周回顾 — 2026-08-29

> 试跑版（非周一定时）；架构级改动等批准后再做。

## 本周事实

- **Autopilot 已上线**：白天 cron `0 6-22`；安静时段 **23:00–06:00**；心跳可补派。
- **派单实测**（约 12 小时内）：
  - `json-site#11` Privacy/Terms → 已 `agent-done`
  - `landing-tool-a#14` Footer → 当前 `agent-running`
  - 队列剩余：`landing-tool-a` #12、#13（minify / Format 快捷键）
- **组合看板此刻**：BLOCKED=0，RUNNING=1，QUEUE=2，MERGED(昨)=0
- **链路**：zstock `:8600` / `agent.revoices.app` / 工作台 `:9477` 均为 200（凌晨曾有 z8600 短抖动，已自愈/拉起）
- **脚本毛刺**：Autopilot 日志出现过 `bg_pid` / `TOTAL_QUEUE` unbound（`set -u` + 通知字符串）；已加固，需观察是否复发
- **Visual Replica Gate / 周回顾 cron**：已落地；本文件即周回顾模板首跑

## 最大摩擦（只挑 1 个）

**本机 `--local` 派单名义上 `max_total=2`，实际在同一个 portfolio 进程里串行跑。**

- 现象：06:11 决定「dispatch up to 2」，但长时间只有 #14 在跑，#12/#13 仍排队。
- 后果：整点/心跳以为「已经开了 2 路」，吞吐被高估；QUEUE 下降偏慢。
- 根因：`dispatch-cursor-agent-cli.sh` 同步等 Agent 结束；portfolio 循环里一张接一张。

## 建议改动（≤3）

1. **`需你点头` — 真并行或「派完即返回」**  
   选项 A：portfolio 对每张票 `nohup` 独立派发，受 `AUTOPILOT_MAX_CONCURRENT` 限制真正并行。  
   选项 B：保持串行，但 Autopilot/`max_total` 文案改成「本轮计划张数（串行）」，并在仪表盘显示「串行中」。  
   推荐先做 **A**（更接近硅谷空闲补位）；若怕打爆本机，并发仍锁 2。

2. **`需你点头` — 最小周指标一页纸自动化**  
   每周一自动汇总：派单次数、agent-done 数、平均 RUNNING 时长、CloudCLI/zstock 5xx 次数。  
   没有数字，进化只会靠感觉。

3. **`可直接做`（已做）— 文档写明串行语义**  
   已在 `employee-autopilot.md` 增加第 8 条：本机 local 多票串行，避免误解。

## 不建议做

- 现在把并发升到 4+（本机易撞车，历史有 Terminated:15）
- 为「看起来忙」加密 cron 到每 15 分钟（不解决串行根因）
- 未 named tunnel 前大改 CloudCLI 架构（先稳 quick tunnel + 缓存）

## 下周一验收点

- [ ] 若批准建议 1A：同一时刻可出现 2 个 `agent-running`（不同 issue）
- [ ] Autopilot 日志无新的 `unbound variable`
- [ ] 周回顾能写出「派/绿/堵」三个数字（即使手工统计）

## 请你拍板

回复例如：`批准 1A` / `批准 1B+2` / `先只观察`。

## 本周脉搏（自动）

> generated: 2026-08-29T10:46:27+08:00 · since merged: `@yesterday` · dispatch: `local-cli-async`

| 指标 | 值 | 说明 |
|------|-----|------|
| 堵 BLOCKED | 1 | 需 CEO 介入 |
| 跑 RUNNING | 0 | agent-running |
| 队 QUEUE | 2 | 可派 agent-safe |
| 绿 MERGED | 0 | cursor/* since @yesterday |
| 活跃项目 | 5 | 非 paused 且可访问 |

$/merged PR：Cursor 账单 ÷ 绿 MERGED（见 [10-cost-and-budget.md](../10-cost-and-budget.md)）。
