name: Prod cluster Deployment

on:
  push:
    branches:
      - main
    paths-ignore:
      - 'gitops/**'
  workflow_dispatch:

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

permissions:
  id-token: write
  contents: read
  attestations: write
  packages: write
  actions: read

concurrency:
  group: ${{ github.ref }}
  cancel-in-progress: true

jobs:
  # The shared agent-runtime base must publish before any image that FROMs it
  # (agentrunner and devenv-runtime). Both downstream bakes default to
  # `BASE_TAG=latest`, so the base bake's `:latest` tag is what they pull in
  # on a fresh build. To pin a specific base into a downstream image, bump
  # BASE_TAG in that image's docker-bake.<image>.hcl to the IMG_SHA tag.
  #
  # Split into per-arch matrix legs because the base image's hermes install
  # crashes uv with SIGSEGV under QEMU x86_64 emulation on arm64 runners.
  # Each leg runs on its native arch, pushes an arch-suffixed tag
  # (:<sha>-amd64 / :<sha>-arm64), and the merge job below stitches them
  # into the canonical :<sha> + :latest multi-arch manifest that downstream
  # FROM ghcr.io/g2crowd/agent-runtime-base:latest builds consume.
  bake-build-agent-runtime-base-arch:
    strategy:
      fail-fast: true
      matrix:
        include:
          - target: default-amd64
            runs-on: ubicloud-standard-4
          - target: default-arm64
            runs-on: ubicloud-standard-4-arm
    uses: g2crowd/gh-actions/.github/workflows/gitops-bake-build.yml@main
    with:
      environment: prod
      branch: ${{ github.head_ref || github.ref_name }}
      bakefile: docker/agent-runtime-base/docker-bake.hcl
      targets: ${{ matrix.target }}
      runs-on: ${{ matrix.runs-on }}
    secrets: inherit

  bake-build-agent-runtime-base:
    needs: [bake-build-agent-runtime-base-arch]
    runs-on: ubicloud-standard-4-arm
    permissions:
      contents: read
      packages: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          # Mirror the reusable bake workflow's checkout so `git rev-parse
          # HEAD` produces the same SHA the per-arch tags were built with.
          ref: ${{ github.head_ref || github.ref_name }}
      - name: Set short git commit SHA
        id: sha
        run: |
          REAL_COMMIT_SHA="$(git rev-parse HEAD)"
          echo "short_sha=${REAL_COMMIT_SHA:0:8}" >> "$GITHUB_OUTPUT"
      - name: Log in to the Container registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
      - name: Create multi-arch manifest from per-arch tags
        env:
          SHORT_SHA: ${{ steps.sha.outputs.short_sha }}
        run: |
          docker buildx imagetools create \
            --tag "ghcr.io/g2crowd/agent-runtime-base:${SHORT_SHA}" \
            --tag "ghcr.io/g2crowd/agent-runtime-base:latest" \
            "ghcr.io/g2crowd/agent-runtime-base:${SHORT_SHA}-amd64" \
            "ghcr.io/g2crowd/agent-runtime-base:${SHORT_SHA}-arm64"
      - name: Inspect resulting manifest
        env:
          SHORT_SHA: ${{ steps.sha.outputs.short_sha }}
        run: |
          docker buildx imagetools inspect \
            "ghcr.io/g2crowd/agent-runtime-base:${SHORT_SHA}"

  bake-build-backend:
    uses: g2crowd/gh-actions/.github/workflows/gitops-bake-build.yml@main
    with:
      environment: prod
      branch: ${{ github.head_ref || github.ref_name }}
      bakefile: docker-bake.backend.hcl
      runs-on: ubicloud-standard-4-arm
    secrets: inherit

  bake-build-web:
    uses: g2crowd/gh-actions/.github/workflows/gitops-bake-build.yml@main
    with:
      environment: prod
      branch: ${{ github.head_ref || github.ref_name }}
      bakefile: docker-bake.web.hcl
      runs-on: ubicloud-standard-4-arm
    secrets: inherit

  bake-build-agentrunner:
    # FROMs agent-runtime-base:latest — base must publish first.
    needs: [bake-build-agent-runtime-base]
    uses: g2crowd/gh-actions/.github/workflows/gitops-bake-build.yml@main
    with:
      environment: prod
      branch: ${{ github.head_ref || github.ref_name }}
      bakefile: docker-bake.agentrunner.hcl
      runs-on: ubicloud-standard-4-arm
    secrets: inherit

  bake-build-devenv-runtime:
    # FROMs agent-runtime-base:latest — base must publish first.
    needs: [bake-build-agent-runtime-base]
    uses: g2crowd/gh-actions/.github/workflows/gitops-bake-build.yml@main
    with:
      environment: prod
      branch: ${{ github.head_ref || github.ref_name }}
      bakefile: docker/devenv-runtime/docker-bake.hcl
      runs-on: ubicloud-standard-4-arm
    secrets: inherit

  extract-tags:
    needs: [bake-build-backend, bake-build-web, bake-build-agentrunner]
    runs-on: ubuntu-latest
    outputs:
      backend_tag: ${{ steps.extract.outputs.backend_tag }}
      web_tag: ${{ steps.extract.outputs.web_tag }}
      agentrunner_tag: ${{ steps.extract.outputs.agentrunner_tag }}
    steps:
      - name: Extract tags from image_name
        id: extract
        env:
          BACKEND_IMAGE: ${{ needs.bake-build-backend.outputs.image_name }}
          WEB_IMAGE: ${{ needs.bake-build-web.outputs.image_name }}
          AGENTRUNNER_IMAGE: ${{ needs.bake-build-agentrunner.outputs.image_name }}
        run: |
          echo "backend_tag=${BACKEND_IMAGE##*:}" >> "$GITHUB_OUTPUT"
          echo "web_tag=${WEB_IMAGE##*:}" >> "$GITHUB_OUTPUT"
          echo "agentrunner_tag=${AGENTRUNNER_IMAGE##*:}" >> "$GITHUB_OUTPUT"

  commit-to-gitops-backend:
    needs: [bake-build-backend, bake-build-web, extract-tags]
    uses: g2crowd/gh-actions/.github/workflows/gitops-deploy.yml@main
    secrets: inherit
    with:
      gitops_repository: g2crowd/agentfarm
      image_tag: ${{ needs.extract-tags.outputs.backend_tag }}
      app_repo_branch: ${{ github.head_ref || github.ref_name }}
      path_to_kustomization: gitops/environments/tools/kustomization.yaml
      image_yq_matcher: .images[0].newTag

  commit-to-gitops-web:
    needs: [bake-build-backend, bake-build-web, extract-tags, commit-to-gitops-backend]
    uses: g2crowd/gh-actions/.github/workflows/gitops-deploy.yml@main
    secrets: inherit
    with:
      gitops_repository: g2crowd/agentfarm
      image_tag: ${{ needs.extract-tags.outputs.web_tag }}
      app_repo_branch: ${{ github.head_ref || github.ref_name }}
      path_to_kustomization: gitops/environments/tools/kustomization.yaml
      image_yq_matcher: .images[1].newTag

  commit-to-gitops-agentrunner:
    needs: [bake-build-agentrunner, extract-tags, commit-to-gitops-web]
    uses: g2crowd/gh-actions/.github/workflows/gitops-deploy.yml@main
    secrets: inherit
    with:
      gitops_repository: g2crowd/agentfarm
      image_tag: ${{ needs.extract-tags.outputs.agentrunner_tag }}
      app_repo_branch: ${{ github.head_ref || github.ref_name }}
      path_to_kustomization: gitops/environments/tools/kustomization.yaml
      image_yq_matcher: .images[2].newTag
