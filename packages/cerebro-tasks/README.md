# @multica/cerebro-tasks

Cross-agent tasks page for the cerebro fork. Lives at `/:workspace/tasks`
and shows every agent task in the workspace with filters for agent,
status, time range, and type (issue / chat). Backed by
`/api/cerebro/tasks` (server/internal/cerebro/tasks).

JEH-900 — Phase 1.
