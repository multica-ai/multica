"use client";

/**
 * First-run starter content is disabled. Keeping this component as a null
 * boundary lets the app layouts remain unchanged while preventing the
 * post-onboarding prompt from creating any starter projects or issues.
 */
export function StarterContentPrompt() {
  return null;
}
