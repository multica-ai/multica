// CEREBRO: bundler override map. Apps register these aliases so cerebro
// packages can shadow upstream modules at build time without forking files.
export const cerebroAliases: Record<string, string> = {};
